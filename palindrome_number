
def isPalindromeInString(self, x: int) -> bool:
    return str(x)==str(x)[::-1

def isPalindrome(self, x: int) -> bool:
    reverse = 0
    num = x
    while x != 0:
        reverse = reverse * 10 + x % 10
        x //= 10
    return reverse == num
